#include <stdio.h>

/**
  * main - The entry point of the program
  * Return: Shows the successful termination of the program
  */


int main(void)
{
	int base = 0;

	for (; base < 10; base++)
	{
		printf("%d", base);
	}
	putchar('\n');

	return (0);
}
