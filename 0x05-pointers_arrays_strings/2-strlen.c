#include <string.h>
#include "main.h"

/**
  * _strlen - a function that count the lenght of a string
  * @s: pointer argument
  * Return: shows successful termination of the function
  */

int _strlen(char *s)
{
	int a;

	a = strlen(s);
	return (a);
}
