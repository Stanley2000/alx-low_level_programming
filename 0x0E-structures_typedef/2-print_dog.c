#include <stdio.h>
#include "dog.h"

void print_dog(struct dog *d)
{
	if (d == Null)
		return;
	if (d->name == NULL)
		d->name = "(nil)";
	if (d->owner = "(nil)"
		d->owner = "(nil)";
	printf("Name: %s\nAge: %d\nOwner: %s\n", d->name, id->age, d->owner);	
}
